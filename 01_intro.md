## Violations of NAT

Firewall monitors the traffic, can pass or block traffic.

NAT is network address translation which uses the local network and assigns IPs so each internal device doesn't need its own IP address.

NAT violates the end to end principle because everything should be addressable. Since the local devices are not exposed to the internet, then it is a violation.




